import { Router } from "express";


const router = Router();

router.put("/editProfile", () => {});
router.get("/addPlaylistToFavorite/:playlistId", () => {});
router.put("/editProfile", () => {});
router.get("/getMyPlaylists", () => {});

export default router;